/**
 * @file ApiConfig.ts
 * @description
 * Este arquivo contém a configuração da URL base para as requisições da API do GitHub.
 *
 * @summary
 * Define a URL base para ser utilizada em todas as requisições HTTP relacionadas à API do GitHub.
 *
 * @constant API_BASE_URL
 * @type {string}
 * @description URL base para a API do GitHub.
 *
 * @example
 * import API_BASE_URL from './ApiConfig';
 * // Use API_BASE_URL para configurar requisições HTTP
 *
 * @note
 * Esta URL base é utilizada em toda a aplicação para fazer requisições à API do GitHub.
 */
const API_BASE_URL = "https://api.github.com";
export default API_BASE_URL;
